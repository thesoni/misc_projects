fcp.py was the first version
parsed the HTML and printed a total

html2csv created a formatted CSV 
to be used to make a graph

plotCSV converts CSV into plot

plot_prac tested out pyplot with dummy data

plotFCP combines all into one script
HTML -> plot   with no intermediate CSV

fcp.html is original HTML page
fcp2.html is just the order data
fcp3.html removed X3/911 data, and is only E46 data

